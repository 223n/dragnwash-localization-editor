package web

import "embed"

// uiFS は画面の資産一式。HTML も CSS も JavaScript も目録もここにある。
//
// 埋めるのは、外から取らないためである。CDN を参照すると、翻訳者の PC から
// 外へ通信が出る。この道具が扱うのはゲームの英語の台本なので、外へ出る経路を
// 1本でも作ると「再配布しない」という前提が崩れる。単一の実行ファイルで
// 完結することは、配布の都合の前に、その前提を守るための条件になっている。
//
//go:embed ui
var uiFS embed.FS
